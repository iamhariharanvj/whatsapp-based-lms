export class Quiz {
  constructor(question, options, rightAnswer, points) {
    this.question = question;
    this.options = options;
    this.rightAnswer = rightAnswer;
    this.points = points;
  }
}
